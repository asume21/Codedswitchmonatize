diff a/client/src/components/producer/BasslineGenerator.tsx b/client/src/components/producer/BasslineGenerator.tsx	(rejected hunks)
@@ -207,1 +207,1 @@
-      audioContextRef.current = new (window.AudioContext || (window as any).webkitAudioContext)();
+      audioContextRef.current = new (window.AudioContext || (window as unknown).webkitAudioContext)();
@@ -245,1 +245,1 @@
-    const masterPlayHandler = (event: any) => {
+    const masterPlayHandler = (event: unknown) => {
@@ -258,1 +258,1 @@
-    return () => window.removeEventListener('masterPlayControl', masterPlayHandler);
+    return () => { window.removeEventListener('masterPlayControl', masterPlayHandler); };
@@ -289,1 +289,1 @@
-      playBassNote(note, bassLengths[stepIndex]);
+      void playBassNote(note, bassLengths[stepIndex]);
@@ -438,1 +438,1 @@
-          onClick={() => setSyncToKick(!syncToKick)}
+          onClick={() => { setSyncToKick(!syncToKick); }}
@@ -469,1 +469,1 @@
-                    onValueChange={(value) => setStepNote(stepIndex, value === '-' ? '' : value)}
+                    onValueChange={(value) => { setStepNote(stepIndex, value === '-' ? '' : value); }}
@@ -496,1 +496,1 @@
-                        onValueChange={(value) => setStepDuration(stepIndex, value[0])}
+                        onValueChange={(value) => { setStepDuration(stepIndex, value[0]); }}
