# Managed by uv from /ace-step/pyproject.toml during Docker build.
# This file is kept for reference only — not used in the build.
#
# Key runtime deps installed via uv sync:
#   torch==2.10.0+cu128, transformers>=4.51.0, accelerate>=1.12.0,
#   diffusers, soundfile, einops, nano-vllm (local), runpod>=1.7.9
