diff a/music-plugin-manager/src/components/studio/plugins/TrackControlsPlugin.tsx b/music-plugin-manager/src/components/studio/plugins/TrackControlsPlugin.tsx	(rejected hunks)
@@ -60,1 +60,1 @@
-                  onClick={() => onTrackSelect(track.id)}
+                  onClick={() => { onTrackSelect(track.id); }}
@@ -70,1 +70,1 @@
-                  onValueChange={(value) => onTrackUpdate(track.id, { instrument: value })}
+                  onValueChange={(value) => { onTrackUpdate(track.id, { instrument: value }); }}
@@ -87,1 +87,1 @@
-                  onClick={() => onTrackUpdate(track.id, { muted: !track.muted })}
+                  onClick={() => { onTrackUpdate(track.id, { muted: !track.muted }); }}
@@ -95,1 +95,1 @@
-                  onClick={() => onTrackUpdate(track.id, { solo: !track.solo })}
+                  onClick={() => { onTrackUpdate(track.id, { solo: !track.solo }); }}
@@ -110,1 +110,1 @@
-                  onValueChange={([value]) => onTrackUpdate(track.id, { volume: value })}
+                  onValueChange={([value]) => { onTrackUpdate(track.id, { volume: value }); }}
