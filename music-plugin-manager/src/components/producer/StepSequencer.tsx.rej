diff a/music-plugin-manager/src/components/producer/StepSequencer.tsx b/music-plugin-manager/src/components/producer/StepSequencer.tsx	(rejected hunks)
@@ -106,1 +106,1 @@
-      audioContextRef.current = new (window.AudioContext || (window as any).webkitAudioContext)();
+      audioContextRef.current = new (window.AudioContext || (window as unknown).webkitAudioContext)();
@@ -291,1 +291,1 @@
-              playDrumSound(track.id, track.velocity[currentStep], track.volume);
+              void playDrumSound(track.id, track.velocity[currentStep], track.volume);
@@ -320,1 +320,1 @@
-    const masterPlayHandler = (event: any) => {
+    const masterPlayHandler = (event: unknown) => {
@@ -358,1 +358,1 @@
-      playDrumSound(track.id, velocity, track.volume);
+      void playDrumSound(track.id, velocity, track.volume);
@@ -381,1 +381,1 @@
-    playDrumSound(track.id, 100, volume);
+    void playDrumSound(track.id, 100, volume);
@@ -491,1 +491,1 @@
-                onClick={() => toggleSolo(trackIndex)}
+                onClick={() => { toggleSolo(trackIndex); }}
